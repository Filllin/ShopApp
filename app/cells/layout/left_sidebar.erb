<div class="col-md-13" id="leftCol">
  <div class="panel panel-default">
    <h4 class="custom-title">Каталог</h4>
    <ul class="nav nav-stacked nav-custom" id="sidebar">
      <% @categories.each do |category| %>
          <% if category.parent_category_id.blank? %>
              <li class="active"><h4><%= link_to category.title, view_category_path(slug: category.slug), class: 'catalog-title' %></h4></li>
              <% category.subcategories.each do |sub_category| %>
                  <li><%= link_to sub_category.title, view_category_path(slug: sub_category.slug), class: 'catalog-subtitle' %></li>
              <% end %>
          <% end %>
      <% end %>
    </ul>
  </div>
  <div class="panel panel-default">
    <h4 class="custom-title">Миссия Friedensbote</h4>
    <div class="center-block">
      <a class="default-width" href="http://www.friedensbote.org" title="истинно говорю вам: так как вы сделали это одному из сих братьев Моих меньших, то сделали Мне">
        <img class="center-block" src="http://www.verlag-friedensbote.de/images/b/160600-01-ru.jpg" alt="истинно говорю вам: так как вы сделали это одному из сих братьев Моих меньших, то сделали Мне">
      </a>
    </div>
  </div>
</div>